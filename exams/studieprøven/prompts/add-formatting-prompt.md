Here’s a detailed prompt you can give an AI agent to transform an unformatted exam (like **test‑1‑before‑formatting.md**) into the richer, structured format found in **test‑1.md**:

---

**Transformation prompt**

Your task is to take a raw exam in Markdown (similar in structure to *test‑1‑before‑formatting.md*) and produce a fully formatted version (like *test‑1.md*) using Markdown and some HTML.  Follow these rules exactly:

1. **Add YAML front matter.** At the very top, insert:

   ```markdown
   ---
   document_title: [exam title with “Practice Test” instead of “Test”]
   ---
   ```

   Replace `[exam title …]` with the actual title of the exam. For example, if the original header is `# Studieprøven - Test 1`, set `document_title` to `Studieprøven - Practice Test 1`. Keep the subject name (e.g., “Studieprøven”) intact and use “Practice Test N” in place of “Test N”.

2. **Create a front page.**

   * Begin the front page with a level‑1 heading formatted like `# Studieprøven _Practice Test 1_`, italicising the “Practice Test” portion with underscores.
   * Immediately after this heading add a logo placeholder: `<div class="logo-wrapper">
  <img class="logo" src="../../../assets/logo-line-drawing-white.png" />
</div>`.
   * Include an **Outline** section summarising the exam structure using bullet points (`-`).  List each major part and its duration, for example:

     ```
     - Læseforståelse (90 minutter)
       - Delprøve 1
       - Delprøve 2A
       - Delprøve 2B
       - Delprøve 3
     - Skriftlig fremstilling (3 timer)
     - Mundtlig kommunikation (30 minutter)
     ```
   * Draw a line below the outline with `<hr class="front-page-line"/>`.
   * Conclude the front page with an italicised disclaimer:

     ```
     _This practice test was generated by Den Frie Prøvebank and is unofficial. Den Frie Prøvebank's ability to validate how well its tests match the official tests, in terms of content and structure, is limited. Users should seek guidance from official teaching institutions as to the structure and content of the exams in question._
     ```
   * Insert a page break after the front page using `<div class="page-break"></div>`.

3. **Section breaks.** Use `<div class="page-break"></div>` before each major part (e.g., before Læseforståelse, before Delprøve 2A answers, before Skriftlig fremstilling, etc.) to start content on a new page.

4. **Læseforståelse questions.**

   * Keep all headings and the body of each text exactly as in the source.
   * After listing the questions, insert `<hr class="line-for-written-answer" />` on its own line after each question to denote answer space.
   * Use another `<div class="page-break"></div>` before moving to the next part.

5. **Gap‑text tasks (removed paragraphs).**

   * Insert an instruction line above the gaps, e.g., `_Sæt bogstavet for den rigtige sætning i hvert felt._`.
   * Replace markers like `[AFSNIT 1 FJERNET]` with bold numbers followed by a blank line (`**(1)** ________`). Do this sequentially for each removed paragraph.
   * At the end of the passage, insert a page break before listing the answer options.

6. **Listing the removed paragraphs (multiple‑choice).**

   * For each removed paragraph (Afsnit 1, Afsnit 2, etc.), start with a numbered heading like `1. Afsnit 1`, `2. Afsnit 2`, and so on.
   * Wrap the options for each Afsnit inside `<ul class="multiple-choice-answers">`. Convert letter‑labelled options (A), B), C)) into numbered list items (1., 2., 3.). Preserve the exact wording.
   * **Do not put long sentences inside tables;** lists are acceptable here.

7. **Standard multiple‑choice questions (e.g., Delprøve 2B).**

   * Precede the question block with an italic instruction, e.g., `_Sæt en cirkel om det rigtige svar._`.
   * After each question, put the answer options in `<ul class="multiple-choice-answers">` using 1., 2., 3. instead of A), B), C).  Keep the text unchanged.

8. **Cloze tasks with word options (e.g., Delprøve 3).**

   * Replace each `**(N)**` marker with a blank in the form `__(N)__ ______`, where `(N)` is still visible and underscores represent missing words.
   * For the answer options, create a horizontal table:

     ```html
     <div class="horizontal-multiple-choice">
     <table class="horizontal-multiple-choice">
       <tbody>
         <tr>
           <td><strong>1</strong></td>
           <td><strong>a.</strong> option1</td>
           <td><strong>b.</strong> option2</td>
           <td><strong>c.</strong> option3</td>
           <td><strong>d.</strong> option4</td>
         </tr>
         <!-- repeat rows for 2, 3, etc. -->
       </tbody>
     </table>
     </div>
     ```

     Use lowercase `a.`, `b.`, etc., with `<strong>` tags. Only include short phrases or single words in each cell; never place full sentences in these table cells.

9. **Skriftlig fremstilling (writing assignment).**

   * Preserve the heading and the bold instruction `**Alle ordbøger er tilladt**`.
   * For each assignment (Opgave 1, 2, 3):

     * Insert a placeholder graphic using Markdown image syntax with descriptive alt text and the filename in parentheses (e.g., `![Forestil dig et diagram der viser boligpriser ...](test-1-boligpriser.png)`).
     * Include the **Manchet** and **Opgave** text exactly as in the source.
   * After presenting the three tasks, insert a series of `<hr class="line-for-written-answer" />` lines to provide room for answers. Use enough lines for several pages if needed, and insert `<div class="page-break"></div>` to break pages cleanly.

10. **Mundtlig kommunikation.**

    * Retain all headings and subsections. Use bullet lists (`-`) for inspiration keywords and enumerated lists (`1.`, `2.`, etc.) for the procedure steps.
    * Place `<div class="page-break"></div>` before this section.
    * Keep assessment criteria as a simple bullet list.

11. **Final disclaimer.** Conclude the document with the same italic disclaimer from the front page.

12. **General requirements.**

    * Do not alter the wording, order, or numbering of any questions or options—only add formatting.
    * Ensure heading levels remain logical (`##` for major parts, `###` for subparts, etc.).
    * Use the provided HTML classes exactly (`logo-wrapper`, `front-page-line`, `page-break`, `line-for-written-answer`, `multiple-choice-answers`, `horizontal-multiple-choice`).
    * Never put long sentences inside any table; tables should contain only short options, single words, or phrases.
    * Insert page breaks (`<div class="page-break"></div>`) to separate major sections and to ensure answer lines start on new pages.

Produce the final, formatted Markdown document adhering to these instructions.

---
